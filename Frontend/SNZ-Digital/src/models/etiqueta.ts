export interface Etiqueta {
    etiquetaId: number;  // ID único para la etiqueta
    nombreEtiqueta: string;      // Nombre de la etiqueta
    detalleEtiqueta: string; // Descripción de la etiqueta (si es necesario)
}
