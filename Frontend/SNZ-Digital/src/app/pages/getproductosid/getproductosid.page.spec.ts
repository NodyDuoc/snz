import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GetproductosidPage } from './getproductosid.page';

describe('GetproductosidPage', () => {
  let component: GetproductosidPage;
  let fixture: ComponentFixture<GetproductosidPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GetproductosidPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
